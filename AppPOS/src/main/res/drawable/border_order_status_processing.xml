<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <stroke
        android:width="@dimen/line"
        android:color="@color/keyboard_bg" />
    <solid android:color="@color/order_status_processing" />
    <corners android:radius="@dimen/order_status_border_radius" />
</shape>